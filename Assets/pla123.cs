using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pla123 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("asdasd");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
